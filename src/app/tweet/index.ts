import { muatations } from "./mutation";
import { queries } from "./querry";
import { resolvers } from "./resolver";
import { types } from "./types";

export const Tweet = { types, muatations, resolvers, queries };
